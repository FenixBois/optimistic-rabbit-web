import { styled } from 'styles';

export const StyledPageTitle = styled('h1', {
    fontWeight: '$bold',
    fontSize: '$pageTitle',
    color: '$primaryHighContrastText',
    paddingTop: '20vh',
});

import type { NextPage } from 'next';
import { PageTitle } from 'components/Layout';

const Home: NextPage = () => {
    return (
        <div>
            <PageTitle>Cookbook</PageTitle>
        </div>
    );
};

export default Home;
